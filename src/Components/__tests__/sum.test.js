import { sum } from "../sum";

test("This method is for testing SUM method wether it is gibing the expected ouput or not",()=>{
    const result = sum(3,4);

    //Assersion
    // expect(result).toBe(7);

})